<Window x:Class="OpenLibrary_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenLibrary_App"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="45"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="175"/>
        </Grid.ColumnDefinitions>
        <TextBox Name="searchBar" Height="35" Margin="10, 5" FontSize="16"/>
        <Button Grid.Column="1" Name="searchButton" Width="75" Height="50" Content="Search" Click="searchButton_Click"/>

        <Viewbox Grid.Row="1">
            <DockPanel  VerticalAlignment="Center" >
                <Label FontWeight="DemiBold">Search By</Label>
                <CheckBox Name="checkAuthor" Margin="5, 5">Author</CheckBox>
                <CheckBox Name="checkTitle" Margin="5, 5">Title</CheckBox>
            </DockPanel>
        </Viewbox>

        <ListView Name="searchResults" Grid.Row="2" Grid.ColumnSpan="2"/>

    </Grid>
</Window>
