<UserControl x:Class="OpenLibrary_App.Views.BookDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenLibrary_App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#f3ffb6" CornerRadius="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="75" />
                <RowDefinition Height="45"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="1" FontSize="16" Text="{Binding Title}"></TextBlock>
            <TextBlock Grid.Column="1" Grid.Row="1"  FontSize="14" Text="{Binding Author}"></TextBlock>
            <TextBlock Grid.Column="1" Grid.Row="2"  FontSize="12" Text="{Binding Description}"></TextBlock>
        </Grid>
    </Border>
</UserControl>
